/**
 * interface for concrete controllers
 * 
 * a sensor calls performSensorAction
 * to make the controller do it's thing
 * 
 * @author tobin
 *
 */
public interface IController {
	void performSensorAction();
}
